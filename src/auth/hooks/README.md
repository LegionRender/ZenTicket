# Auth Hooks Directory

This folder is reserved for custom React hooks related to session and authentication management.
Examples:
- `useAuth`: accesses authenticated user context.
- `useRequireAuth`: redirects users if they are not logged in.
